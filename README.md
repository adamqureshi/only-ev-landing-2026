# Only EV — Simple Landing (GitHub Pages)

This is a *descoped* static landing page with a grey background and black text.
It also attempts to embed: https://adamqureshi.com/only-ev/

> Note: some sites block embedding via browser security headers. If the embed doesn't load,
> the page includes a direct link to open it in a new tab.

## Files
- `index.html`
- `assets/styles.css`
- `assets/favicon.svg`
- `.nojekyll`

## Custom domain
If your repo already has a `CNAME` file (for onlyev.com), **keep it** when replacing files.
